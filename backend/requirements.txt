# Production dependencies. Upper bounds cap the major version so a breaking
# release of FastAPI/Pydantic/etc can't ship to production on the next deploy
# without a commit here. Test tooling lives in requirements-dev.txt.
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
pydantic>=2.7.0,<3.0
pydantic-settings>=2.3.0,<3.0
httpx>=0.27.0,<1.0
numpy>=1.26.0,<3.0
