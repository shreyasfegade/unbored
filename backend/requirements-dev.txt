# Dev/CI-only dependencies. Not installed in production (Render runs
# `pip install -r requirements.txt`), so pytest never ships to the server.
-r requirements.txt
pytest>=8.2.0,<9.0
pytest-asyncio>=0.23.0,<1.0
